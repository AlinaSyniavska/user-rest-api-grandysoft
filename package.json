{
  "scripts": {
    "start": "node app.js",
    "dev": "pm2 start app.js --name mainServer --watch --attach",
    "kill": "pm2 kill",
    "prisma-init": "npx prisma init npx prisma migrate dev --name init",
    "prisma-seed": "npx prisma db seed"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.7.0",
    "pm2": "^5.2.2"
  },
  "devDependencies": {
    "prisma": "^4.6.1"
  }
}
